<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppAg13_DS_II___Hotel.views.ContratacaoHospedagem"
             Title="ContratacaoHospedagem"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="GreenYellow">
    
    <VerticalStackLayout VerticalOptions="Center" Padding="10">
        <Label 
            Text="Cálculo da Diária"
            FontFamily="Bauhaus-93Regular" FontSize="45" TextColor="Black" />
       
        <Frame Padding="10">
            <VerticalStackLayout>
                <Grid RowDefinitions="Auto, Auto"
                      ColumnDefinitions="*, *, 2*">
                    
                    <Label Grid.Row="0" Grid.Column="0"
                           Text="Adultos: " />

                    <Label Grid.Row="0" Grid.Column="1"
                           TextColor="Violet"
                           Text="{Binding Value}"
                           BindingContext="{x:Reference stp_adultos}"/>

                    <Stepper Grid.Row="0" Grid.Column="2"
                             x:Name="stp_adultos"
                             Minimum="0"
                             Maximum="10"
                             Increment="1" />

                    <Label Grid.Row="1" Grid.Column="0"
                           Text="Criancas:"/>

                    <Label Grid.Row="1" Grid.Column="1"
                           TextColor="Violet" 
                           Text="{Binding Value}"       
                           BindingContext="{x:Reference stp_criancas}"/>
                    
                    <Stepper Grid.Row="1" Grid.Column="2"
                               x:Name="stp_criancas"
                               Minimum="0"
                               Maximum="10"
                               Increment="1" />
                </Grid>

                <Label Text=" SUITE"  FontFamily="Kalam-Bold" />
                
                <Picker x:Name="pck_quarto"
                        Title="Serlecione sua Acomodação"
                        ItemDisplayBinding="{Binding Descricao}"
                />
                <Frame Padding="10" 
                       Margin="10">

                    <VerticalStackLayout>
                        <Label Text="Periodo da Estadia"  FontFamily="Kalam-Bold"  />
                        
                        <HorizontalStackLayout>
                            <Label Text="Check-in:" />
                            <DatePicker x:Name="dtpck_checkin"
                                        DateSelected="dtpck_checkin_DateSelected"                                        
                                         Format="dd/MM/yyyy"/>                            
                        </HorizontalStackLayout>
                        
                        <HorizontalStackLayout>
                            <Label Text="Check-out:" />
                            <DatePicker x:Name="dtpck_checkout"
                                         Format="dd/MM/yyyy"/>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                                      
                    
                </Frame>

                <ImageButton Source="dseta.png"
                Clicked="ImageButton_Clicked"
                BackgroundColor="Transparent"           
                WidthRequest="100"
               HeightRequest="100"
                HorizontalOptions="End"
                VerticalOptions="Center"/>
                

            </VerticalStackLayout>  
            
        </Frame>
        <ImageButton Source="sobre.png"
             BackgroundColor="Transparent"           
             WidthRequest="20"
             HeightRequest="100"
             HorizontalOptions="Start"
             VerticalOptions="Center"
             Clicked="OnSobreClicked"/>
        
        
    </VerticalStackLayout>
    
</ContentPage>